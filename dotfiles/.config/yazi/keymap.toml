# Add a few bookmarks without replacing Yazi's built-in keymap.

[mgr]
prepend_keymap = [
  { on = [ "g", "h" ], run = "cd ~",           desc = "Go home" },
  { on = [ "g", "w" ], run = "cd ~/workspace", desc = "Go workspace" },
  { on = [ "g", "c" ], run = "cd ~/.config",   desc = "Go config" },
]
